task :default do
  puts "rake server"
end

task :server do
  require 'bundler/setup'
  require 'rack/handler/puma'
  require_relative 'rackblog'

  Rack::Handler::Puma.run Rackblog.new, {Port: 8081}
end